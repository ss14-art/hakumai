- type: entity
  abstract: true
  parent: [BaseItem, PowerCellSlotSmallItem]
  id: BaseHandheldComputer
  components:
  - type: ActivatableUIRequiresPowerCell
  - type: ItemToggle
    onUse: false # above component does the toggling
  - type: PowerCellDraw
    drawRate: 0
    useCharge: 20
  - type: ToggleCellDraw
  - type: PhysicalComposition
    materialComposition:
      Steel: 50 # Just as a baseline for other items
